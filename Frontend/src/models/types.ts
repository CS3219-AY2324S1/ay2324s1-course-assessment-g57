export type Fact = { fact: string; length: number };
export type User = { id: number; name: string; username: string; email: string };
export type Question = {id: number; description: string; difficulty: number; category: string;}
